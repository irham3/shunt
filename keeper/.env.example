# testnet | mainnet
STELLAR_NETWORK=testnet
# HORIZON_URL=https://horizon-testnet.stellar.org
# SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
# ShuntVault contract id (C...)
VAULT_CONTRACT_ID=
# Circle USDC issuer (defaults per network are built in)
# USDC_ISSUER=
# Comma-separated G... accounts to watch for inflows
WATCH_ACCOUNTS=
PORT=8787
STATE_FILE=./keeper-state.json
